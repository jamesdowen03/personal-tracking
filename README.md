# personal-tracking

This project is aimed to allow users to create an entry that they want to have tracked. It will:
  1) Ask the user the time period
  2) Ask the user when they would like to be notified
  3) Allow the user to change the screen depending on the entry timeline.
